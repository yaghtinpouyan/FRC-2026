package frc.robot.subsystems;
import edu.wpi.first.wpilibj2.command.SubsystemBase;

import java.util.ArrayList;
import java.util.List;

public class BoundaryPF extends SubsystemBase {
  private Drive odometry = Drive.getInstance();

  public static class Point {
    public double x, y;
    public Point(double x, double y) { this.x = x; this.y = y; }
  }

  public static class Boundary {
    private final List<Point> vertices;

        public createBoundary() {
            List<Point> vertices = List.of(
            new Point(0, 0), //arbitary points
            new Point(4, 0),
            new Point(4, 3),
            new Point(0, 3)
        );

    // code to check if robot is in the boundary, predicted position etc
  }
}
